# JSON Files in the Shared Directory
This directory contains JSON files that are shared between all projects in the workspace. It contains an options folder with different locale version of the texts
